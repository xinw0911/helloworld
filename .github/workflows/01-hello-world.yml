name: hello-world
on: push
jobs:
  my-job:
    runs-on: self-hosted
    steps:
      - name: hello-word-step
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v2
        with:
          distribution: 'adopt'
          java-version: '11'
          check-latest: true
      - run: echo "Hello World!"
      - run: javac -cp . HelloWorld.java
      - run: echo "Done!"
